#!/bin/bash

ROOTPATH=$( cd "$(dirname ./setup)"; cd .. ; pwd -P )

export PATH=$PATH:$ROOTPATH/bin
export JJJRMI=$ROOTPATH

echo "#!/bin/bash" > jjjrmi
echo "java -classpath $ROOTPATH/target/dependency/*:$ROOTPATH/target/classes ca.frar.jjjrmi.CLI $@" >> jjjrmi