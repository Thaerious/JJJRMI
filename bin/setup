#!/bin/bash

ROOTPATH=$( cd "$(dirname ./$BASH_SOURCE)"; cd .. ; pwd -P )

export PATH=$PATH:$ROOTPATH/bin
echo $ROOTPATH

cp bin/jjjrmi.template bin/jjjrmi
sed -i "s|ROOTPATH=|ROOTPATH=$ROOTPATH|" "bin/jjjrmi"